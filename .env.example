# =====================================================
# DOCKER COMPOSE SHARED SERVICES CONFIGURATION
# Only contains settings needed by multiple containers
# =====================================================

# Database Configuration (used by db service and backend)
DB_NAME=db_name_here
DB_USER=db_user_here
DB_PASSWORD=db_password_here

# Redis Configuration (used by redis service and backend)
REDIS_PASSWORD=redis_password_here